# Win Install

PowerShell + winget scripts to install apps and apply registry tweaks on a fresh Windows machine. Config in `apps.yaml` and `win-setup-config.json`.

```powershell
.\win-install.ps1
```
